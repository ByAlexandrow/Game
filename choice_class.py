def choice(user_choice, a, b, c):
    """Функция корректирует значение здоровья героя."""
    if user_choice == a:
        result = a
    elif result == b:
        result = b
    else:
       result = c
    
    return result
